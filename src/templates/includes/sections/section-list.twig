<section class=" section section-list">
    <div class="container">
        <div class="content">
            <h2 class="title">{{listSection.title}}</h2>
            <ul class="list">
                {% for item in listSection.list %}
                {% include '@root/includes/components/list-item.twig' with {
                text:item
                }%}
                {% endfor %}
            </ul>
        </div>
        {# {% include '@root/includes/components/attention.twig' %} #}
    </div>
</section>