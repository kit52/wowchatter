<section class=" section section-chats">
    <div class="container">
        <div class="content">
            <h2 class="title">Chats</h2>
            <div class="cards">
                {% for card in cards %}
                {% include '@root/includes/components/chat-card.twig' with {
                card
                } %}
                {% endfor %}
            </div>
        </div>

    </div>
</section>